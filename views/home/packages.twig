<section class="packages text-center" style="background-image: url({{TimberImage(post.get_field('package_bg')).src}})">
  <div class="row">
  <div class="xlarge-6 large-8 medium-10 small-centered white-panel columns">
    <div class="title">{{post.get_field('package_tag')}}</div>
    <h2>{{post.get_field('package_title')}}</h2>
    <hr>
    <p>{{post.get_field('package_desc')}}</p>
    <a href="{{site.url}}/packages" class="button coral">{{post.get_field('package_button')}}</a>
  </div> <!-- columns -->
  </div>
</section>
