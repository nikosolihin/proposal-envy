header {
  background-image: url({{TimberImage(post.get_field('header_bg')).src}});
}
.about-role {
  background-image: linear-gradient(rgba(36, 32, 56, .85),rgba(36, 32, 56, .85)),url({{TimberImage(post.get_field('role_background')).src}});
}
.arista {
  background-image: url({{TimberImage(post.get_field('arista_photo')).src}});
}
.handy {
  background-image: url({{TimberImage(post.get_field('handy_photo')).src}});
}